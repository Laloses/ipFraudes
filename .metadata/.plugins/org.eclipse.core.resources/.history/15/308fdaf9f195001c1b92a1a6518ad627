package com.ibm.academia.restapi.ipfraude.services;

import static org.assertj.core.api.Assertions.assertThat;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

import com.ibm.academia.restapi.ipfraude.modelo.dto.CountryLayerDTO;
import com.ibm.academia.restapi.ipfraude.modelo.dto.Ip2CountryDTO;
import com.ibm.academia.restapi.ipfraude.modelo.servicios.CountryServiceImpl;

@SpringBootTest
public class Ip2CountryServiceImplTest {
	
	@Autowired
	private CountryServiceImpl countryService;
	
	@Test
	@DisplayName("Buscando con un codigo alpha correcto")
	void findCountryByIP() {
		//GWT
		//Given
		String code = "AFG";
		//When
		CountryLayerDTO expected = countryService.findCountryByCode3(code);
		//Then
		assertThat(expected.getMoneda()).isEqualTo("Afghan afghani");
		assertThat(expected.getCotizacion()).isEqualTo("AFN");
	}
	
	@Test
	@DisplayName("Buscando con un codigo alpha incorrecto o no existente")
	void findCountryByIPBadIP() {
		//GWT
		//Given
		String code = "AFGasdas";
		//When
		CountryLayerDTO expected = countryService.findCountryByCode3(code);
		//Then
		assertThat(expected.getMoneda()).isEqualTo(null);
		assertThat(expected.getCotizacion()).isEqualTo(null);
	}
}
